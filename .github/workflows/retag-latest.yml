name: Retag latest (DEPRECATED)

# This workflow is deprecated. Use npm-package.yml with retag_latest=true instead.
# Kept for reference only - do not use.

on:
  workflow_dispatch:
    inputs:
      _note:
        description: 'DEPRECATED: Use npm-package.yml with retag_latest=true instead'
        required: false

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Fail with deprecation message
        run: |
          echo "ERROR: This workflow is deprecated."
          echo "Use 'npm package' workflow with retag_latest=true instead."
          exit 1
